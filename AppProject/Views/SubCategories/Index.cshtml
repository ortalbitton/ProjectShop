@model IEnumerable<AppProject.Models.SubCategory>

@{
    ViewData["Title"] = "Index";
}


<head>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script type="text/javascript">
    </script>
</head>

<ul class="Component">
    @foreach (var item in Model)
    {
        <li class="ComponentItem"><div class="Text"><a id="@item.SubCategoryId" onclick="ProductList()">@item.SubName</a></div></li>
    }
</ul>


@*<h2>Index</h2>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.SubName)
                    </th>
                <th>
                    @Html.DisplayNameFor(model => model.Categories)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.SubName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Categories.CategoryId)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.SubCategoryId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.SubCategoryId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.SubCategoryId">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>*@
